bin/
obj/
*.user
.vs/
TestResults/
.claude/
# Packages are distributed through nuget.org, not the repository.
*.nupkg
# Screenshots were taken on real prospect installs and must never reach the public repo.
docs/*.png
# Recordings and packages are not source.
*.mp4
*.webm
artifacts/*.png
# Unpacked nupkg used to inspect a build. Output, not source; regenerated on demand.
artifacts/extracted/
